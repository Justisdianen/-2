# 若依管理系统 - 项目启动说明

## 一、技术栈

### 后端技术栈
| 技术 | 版本 | 说明 |
|------|------|------|
| Java | 17+ | 开发语言 |
| Spring Boot | 3.2.5 | 后端框架 |
| MyBatis-Plus | 3.5.5 | ORM框架 |
| MySQL | 8.0+ | 数据库 |
| Druid | 1.2.21 | 数据库连接池 |
| Lombok | 1.18.32 | 代码简化工具 |

### 前端技术栈
| 技术 | 版本 | 说明 |
|------|------|------|
| Vue | 3.4.21 | 前端框架 |
| Vue Router | 4.3.0 | 路由管理 |
| Element Plus | 2.6.3 | UI组件库 |
| Axios | 1.6.8 | HTTP客户端 |
| Vite | 5.2.8 | 构建工具 |

## 二、环境要求

### 基础环境
- JDK 17 或更高版本
- Node.js 18 或更高版本
- MySQL 8.0 或更高版本
- Maven 3.8+

### 环境变量配置
1. **JAVA_HOME**: JDK安装路径
2. **NODE_HOME**: Node.js安装路径
3. **MAVEN_HOME**: Maven安装路径

## 三、项目结构

```
1111/
├── backend/                    # 后端项目
│   ├── src/main/java/com/example/ruoyi/
│   │   ├── controller/         # 控制器层
│   │   ├── service/            # 服务层
│   │   ├── mapper/             # 数据访问层
│   │   ├── entity/             # 实体类
│   │   ├── config/             # 配置类
│   │   ├── common/             # 通用模块
│   │   └── RuoYiApplication.java
│   ├── src/main/resources/
│   │   ├── mapper/             # MyBatis映射文件
│   │   └── application.yml     # 应用配置
│   └── pom.xml                 # Maven依赖配置
├── frontend/                   # 前端项目
│   ├── src/
│   │   ├── api/                # API接口封装
│   │   ├── views/              # 页面组件
│   │   ├── router/             # 路由配置
│   │   ├── utils/              # 工具函数
│   │   ├── components/         # 公共组件
│   │   ├── assets/             # 静态资源
│   │   ├── App.vue             # 根组件
│   │   └── main.js             # 入口文件
│   ├── index.html              # HTML模板
│   ├── vite.config.js          # Vite配置
│   ├── package.json            # 依赖配置
│   ├── .env.development        # 开发环境配置
│   └── .env.production         # 生产环境配置
├── init_table.sql              # 数据库初始化脚本
├── 系统需求.md                  # 系统需求文档
└── 启动说明.md                  # 项目启动说明
```

## 四、数据库配置

### 4.1 创建数据库

打开MySQL客户端，执行以下命令：

```sql
CREATE DATABASE IF NOT EXISTS example_db 
DEFAULT CHARACTER SET utf8mb4 
DEFAULT COLLATE utf8mb4_unicode_ci;
```

### 4.2 导入初始化数据

```bash
mysql -u root -p example_db < init_table.sql
```

### 4.3 修改数据库连接配置

编辑 `backend/src/main/resources/application.yml`：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/example_db?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: your_password
    driver-class-name: com.mysql.cj.jdbc.Driver
```

将 `password` 替换为你的MySQL密码。

## 五、后端启动

### 5.1 进入后端目录

```bash
cd backend
```

### 5.2 编译项目

```bash
mvn clean compile
```

### 5.3 运行项目

**方式一：使用Maven运行**

```bash
mvn spring-boot:run
```

**方式二：打包后运行**

```bash
mvn clean package
java -jar target/ruoyi-admin-1.0.0.jar
```

### 5.4 验证启动

访问 http://localhost:8080 ，看到以下内容表示启动成功：

```json
{
  "code": 404,
  "msg": "Not Found",
  "data": null
}
```

## 六、前端启动

### 6.1 进入前端目录

```bash
cd frontend
```

### 6.2 安装依赖

```bash
npm install
```

### 6.3 启动开发服务器

```bash
npm run dev
```

### 6.4 访问前端

打开浏览器访问 http://localhost:5173

## 七、配置说明

### 7.1 后端配置说明

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| server.port | 8080 | 服务端口 |
| spring.datasource.url | jdbc:mysql://localhost:3306/example_db | 数据库连接地址 |
| spring.datasource.username | root | 数据库用户名 |
| spring.datasource.password | password | 数据库密码 |
| mybatis-plus.mapper-locations | classpath*:mapper/**/*.xml | MyBatis映射文件路径 |

### 7.2 前端配置说明

**开发环境 (.env.development)**

```bash
VITE_APP_TITLE=若依管理系统
VITE_APP_API_BASE_URL=/prod-api
```

**生产环境 (.env.production)**

```bash
VITE_APP_TITLE=若依管理系统
VITE_APP_API_BASE_URL=/prod-api
```

**Vite代理配置 (vite.config.js)**

```javascript
proxy: {
  '/prod-api': {
    target: 'http://localhost:8080',
    changeOrigin: true,
    rewrite: (path) => path.replace(/^\/prod-api/, '')
  }
}
```

前端请求 `/prod-api/system/user/list` 会被代理到 `http://localhost:8080/system/user/list`。

## 八、默认登录信息

| 用户名 | 密码 | 角色 |
|--------|------|------|
| admin | admin123 | 超级管理员 |
| user | admin123 | 普通用户 |

## 九、接口文档

### 9.1 用户管理接口

| HTTP方法 | 路径 | 说明 |
|----------|------|------|
| GET | /system/user/list | 用户列表查询 |
| POST | /system/user | 新增用户 |
| PUT | /system/user | 修改用户 |
| DELETE | /system/user/{userId} | 删除用户 |

### 9.2 角色管理接口

| HTTP方法 | 路径 | 说明 |
|----------|------|------|
| GET | /system/role/list | 角色列表查询 |
| POST | /system/role | 新增角色 |
| PUT | /system/role | 修改角色 |
| DELETE | /system/role/{roleId} | 删除角色 |
| GET | /system/role/authMenu/{roleId} | 获取角色菜单权限 |
| PUT | /system/role/authMenu | 分配角色菜单权限 |

### 9.3 菜单管理接口

| HTTP方法 | 路径 | 说明 |
|----------|------|------|
| GET | /system/menu/list | 菜单列表查询 |
| POST | /system/menu | 新增菜单 |
| PUT | /system/menu | 修改菜单 |
| DELETE | /system/menu/{menuId} | 删除菜单 |
| GET | /system/menu/tree | 获取菜单树形结构 |

### 9.4 部门管理接口

| HTTP方法 | 路径 | 说明 |
|----------|------|------|
| GET | /system/dept/list | 部门列表查询 |
| POST | /system/dept | 新增部门 |
| PUT | /system/dept | 修改部门 |
| DELETE | /system/dept/{deptId} | 删除部门 |

## 十、常见问题

### 10.1 MySQL连接失败

**问题描述**: 启动后端时提示连接数据库失败

**解决方案**:
1. 检查MySQL服务是否启动
2. 确认数据库连接地址、用户名、密码正确
3. 确认MySQL端口3306未被占用
4. 确认数据库 `example_db` 已创建

### 10.2 端口冲突

**问题描述**: 启动时提示端口已被占用

**解决方案**:
- 修改 `application.yml` 中的 `server.port`
- 或停止占用端口的进程

### 10.3 前端代理失败

**问题描述**: 前端请求接口时404或跨域

**解决方案**:
1. 确认后端服务已启动在8080端口
2. 确认 `vite.config.js` 中的代理配置正确
3. 确认浏览器没有缓存旧的代理配置

### 10.4 前端页面空白

**问题描述**: 访问前端页面显示空白

**解决方案**:
1. 打开浏览器开发者工具查看控制台错误
2. 确认 `npm install` 已成功执行
3. 确认 `npm run dev` 没有报错

## 十一、项目构建

### 11.1 后端构建

```bash
cd backend
mvn clean package
```

构建产物位于 `backend/target/ruoyi-admin-1.0.0.jar`

### 11.2 前端构建

```bash
cd frontend
npm run build
```

构建产物位于 `frontend/dist/` 目录

## 十二、生产部署

### 12.1 后端部署

```bash
java -jar target/ruoyi-admin-1.0.0.jar --spring.profiles.active=prod
```

### 12.2 前端部署

将 `dist/` 目录部署到Nginx或其他静态资源服务器。

**Nginx配置示例**:

```nginx
server {
    listen 80;
    server_name your_domain;

    location / {
        root /path/to/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location /prod-api/ {
        proxy_pass http://localhost:8080/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

---

**文档版本**: v1.0  
**生成时间**: 2026-06-26