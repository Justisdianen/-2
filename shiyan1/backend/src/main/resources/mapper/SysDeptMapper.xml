<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ruoyi.mapper.SysDeptMapper">

    <resultMap type="com.example.ruoyi.entity.SysDept" id="SysDeptResult">
        <id property="deptId" column="dept_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="deptName" column="dept_name"/>
        <result property="leader" column="leader"/>
        <result property="phone" column="phone"/>
        <result property="orderNum" column="order_num"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="ancestors" column="ancestors"/>
    </resultMap>

    <sql id="selectDeptVo">
        select dept_id, parent_id, dept_name, leader, phone, order_num, status, del_flag, create_by, create_time, update_by, update_time, ancestors
        from sys_dept
    </sql>

    <select id="selectDeptList" parameterType="com.example.ruoyi.entity.SysDept" resultMap="SysDeptResult">
        <include refid="selectDeptVo"/>
        where del_flag = '0'
        <if test="deptName != null and deptName != ''">
            and dept_name like concat('%', #{deptName}, '%')
        </if>
        <if test="status != null and status != ''">
            and status = #{status}
        </if>
        order by order_num
    </select>

    <select id="selectDeptIdsByRoleId" parameterType="Long" resultType="Long">
        select dept_id
        from sys_role_dept
        where role_id = #{roleId}
    </select>

</mapper>